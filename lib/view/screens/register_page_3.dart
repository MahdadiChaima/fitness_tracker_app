import 'package:flutter/material.dart';
class Registerage3 extends StatelessWidget {
  const Registerage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
